/* Intro Section */

#intro {
    background-color: black;
    text-align: center;
    padding: 100px 0;
}
#intro a {
    text-decoration: none; /* Remove the underline */
    color: inherit; /* Inherit the text color from the parent element */
}

#intro h1 {
    font-size: 5em; /* Adjust the font size as needed */
    color: rgb(241, 240, 240); /* Set the text color to red */
    margin: 0;  /* Remove default margin for h1 */
}

/* Rest of your existing CSS styles */

/* Intro Section */
#intro {
    background-color: black;
    text-align: center;
    padding: 20px 0;
    font-family: 'quicksilver', sans-serif;
}



body, h1, h2, ul, li, p { margin: 0; padding: 0; }

/* Basic styling */
body { font-family: 'quicksilver', sans-serif; }

header {
    background: black;
    color: #fff;
    padding: 10px 0;
}

nav ul { list-style: none; text-align: center; }

nav ul li { display: inline; margin: 0 15px; }

nav ul li a { text-decoration: none; color: #fff; }

section { padding: 40px; }

h1, h2 { color: #333; }

footer {
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%; /* Set the width to 100% to span the entire viewport */
    background: black;
    color: #fff;
    text-align: center;
    padding: 10px 0;
}

/* Video Background */
#video-bg, .overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    object-fit: cover; /* Add this line to ensure the video covers the entire element */
}


.overlay {
    background: rgba(0, 0, 0, 0.5); /* Adjust the opacity as needed */
}

#video-caption {
    position: absolute;
    top: 133px; /* Adjust the top distance as needed */
    right: 0px; /* Adjust the right distance as needed */
    color: #fff;
    font-size: 40px; 
    font-family: 'paskowy', sans-serif; 
    z-index: 2; /* Ensure it's above the video */
}

/* Parallax Container Styles */
.parallax-container {
    overflow-x: hidden;
    position: relative;
    display: flex; /* Use flex to remove gaps between strips */
    flex-direction: column; /* Stack the strips vertically */
}

/* Parallax Effect on Video Background */
#video-bg {
    min-width: 100%;
    min-height: 100%;
    object-fit: cover;
    position: fixed;
    top: 0;
    left: 0;
    z-index: -1;
}

/* Add your existing CSS styles here */

/* Strip Backgrounds */
.strip {
    height: 100vh;
    background-position: 0% 0%;
    background-size: cover;
    background-repeat: no-repeat;
    background-attachment: fixed;
    position: relative; /* Ensure relative positioning for child elements */
}


#strip1 {
    background-image: url('strip1.png');
}

#strip2 {
    background-image: url('strip2.png');
}

#strip3 {
    background-image: url('strip3.png');
}

#strip4 {
    background-image: url('strip4.png');
}


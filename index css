/* Slideshow container */
.slideshow-container {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
    width: 100%;
}

/* Hide images by default */
.mySlides {
    display: none;
    transform: translateX(100%); /* Start off-screen to the right */
    transition: opacity 1.5s, transform 0.5s /* Smooth cubic-bezier transition */
}

/* Center the slideshow content */
.slideshow-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh; /* Ensure the container covers the entire viewport height */
    
   
}

.slideshow-wrapper {
    text-align: center;
    
}

/* Style the center-text div */
.center-text {
    text-align: center;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
}

/* Style the phrase */
.center-text h1 {
    font-family: 'lemon milk', sans-serif; /* Use the Quicksilver font */
    font-size: 36px; /* Adjust font size as needed */
    color: red;
}

/* ... Existing CSS styles ... */


/* Style the "LA MORT EST BELLE" text */
#cover-content {
    text-align: center;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 2;
}

/* Style the "ENTER" button */
.enter-button {
    display: block;
    text-align: center;
    font-size: 24px;
    font-family: 'Quicksilver', sans-serif;
    color: black;
    text-decoration: none;
    position: absolute;
    bottom: 20px; /* Adjust the bottom spacing as needed */
    left: 50%;
    transform: translateX(-50%);
    transition: color 0.3s;
}

.enter-button:hover {
    color: red;

}
/* Style the button container */
.button-container {
    text-align: center;
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
}
/* ... Existing CSS styles ... */


/* Style the images (adjust dimensions as needed) */
.mySlides img {
    width: 50%;
    height: auto;
}

/* Previous and Next buttons */
.prev,
.next {
    cursor: pointer;
    position: absolute;
    top: 50%;
    width: auto;
    padding: 16px;
    margin-top: -22px;
    color: white;
    font-weight: bold;
    font-size: 24px;
    transition: 0.6s ease;
}

/* Position the Previous button on the left */
.prev {
    left: 0;
}

/* Position the Next button on the right */
.next {
    right: 0;
}

/* On hover, add a grey background color */
.prev:hover,
.next:hover {
    background-color: rgba(0, 0, 0, 0.8);
}



#cover-content h1 {
    font-size: 80px; /* Adjust the font size as needed */
    color: red;
    font-family: 'Quicksilver', sans-serif; 
    white-space: nowrap; /*prevents line from cutting off :D */
    transform: none;
  
    
}
